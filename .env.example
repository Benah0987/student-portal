# Example environment variables for production
# Copy this to a real .env (don't commit secrets) and set values in your host (Render)

SECRET_KEY=change-me-to-a-secure-value
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,localhost,127.0.0.1

# If using Postgres (recommended)
# DATABASE_URL in the form: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://user:password@db-host:5432/dbname

# Static and media (if using a custom path)
STATIC_ROOT=/static
MEDIA_ROOT=/media
